<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skydt.weightcontrol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainMenuActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.InformationActivity"
            android:label="@string/information"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.GuideActivity"
            android:label="@string/guide"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.RegisterWeightActivity"
            android:label="@string/indtast_vægt"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.DietSelectionActivity"
            android:label="@string/vælg_diæt"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.DietCreationActivity"
            android:label="@string/opret_diæt"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.DayActivity"
            android:label="@string/dag_info"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.DietActivity"
            android:label="@string/diæt_info"
            android:screenOrientation="portrait" />
    </application>

</manifest>